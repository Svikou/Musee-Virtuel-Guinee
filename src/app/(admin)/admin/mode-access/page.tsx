import { SectionCards } from "@/components/dashboard/CardSection";
import React from "react";

const page = () => {
  return (
    <div className="@container/main flex flex-1 flex-col gap-2">
      <SectionCards />
    </div>
  );
};

export default page;
